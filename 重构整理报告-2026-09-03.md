# 重构整理报告（2026-09-03）

## 交付范围

- 交付完整源码、本报告，以及 `release/` 中现有的最新版 NAS、Windows 和 macOS 安装包。
- 本次只做行为不变的源码整理，未重新打包；Windows 保留已完成的 1.3.2 教师端与学生端，macOS 保留当前最新可用的 1.1.1 ARM64 学生端。
- `release/.env` 按要求原样保留；运行数据 `server/data/` 也未作为中间文件删除。

## 已完成

- 统一服务端、Web 端、桌面端及校验脚本的多行排版，保留原有 API、数据库字段、错误文案和流程。
- 将过重职责拆到既有业务边界：下载、预览图、分片上传、资料、成绩汇总、学生移除等分别由独立路由、服务或组件承接。
- 合并重复的资料查询入口，删除未使用的 Vue 生命周期导入，并清理无意义的本地函数别名。
- 将源码和模板中的单字母、缩写及含义不清的业务变量改为自解释名称，包括 `assignmentForm`、`savedConfig`、`formatScore`、`material`、`submissionRow` 和 `temporaryDirectory`。
- 删除描述代码表面行为、自我辩护或引用阶段编号的注释；保留并改写与权限、事务、兼容、隐私、清理风险相关的原因说明。
- 合并前已将全部 21 个未跟踪源码文件纳入 Git 暂存区；最终没有遗漏的未跟踪文件或未暂存修改。
- 新增前后端成绩计算一致性测试，直接执行两端现有公式并比较同一批场景的结果，不调整现有实现结构。

## 保留原状、需要确认

以下逻辑看似重复或偏防御性，但删除可能改变并发、升级或失败恢复行为，本次未动：

1. 资料上传在路由预检和幂等操作执行阶段各做一次权限检查。是否只保留一次，需要明确可接受的并发权限变更窗口。
2. 数据库旧绝对文件路径与新相对存储键的双格式读取兼容。是否删除旧兼容，需要先确认所有部署都已完成迁移。
3. 中断/失败上传的延迟清理、被引用文件保留、孤儿文件隔离区及过期删除。是否缩减，需要明确故障恢复和误删风险口径。
4. 客户端取消上传后继续查询服务端结果、相同请求编号的幂等重放与冲突检查。是否简化，需要明确弱网和重复点击场景是否仍受支持。

## 验证

- 后端：63 项自动化测试全部通过，包括新增的前后端成绩计算一致性测试。
- Web：生产构建通过。
- 静态检查：未发现残留的指定缩写、单字母局部声明或单字母业务函数参数。
- 四个保留安装包的 SHA-256 均与 `release/SHA256SUMS.txt` 一致。
- Git：134 个路径已暂存；未跟踪文件与未暂存修改均为 0。

## 工作区清理

- 已删除前端 `dist`、桌面端 `node_modules`、桌面端解包/打包目录、blockmap 和中断构建残留。
- 根目录依赖已按锁文件恢复并保留，供后续重构、测试和前端验证继续使用。
- 保留锁文件与全部构建配置，因此依赖和构建产物仍可按原流程重建。
- 保留约 1.3 MB 的 `.mimosa` 工作缓存；其体积很小且可能包含工具工作状态。

## 代码审查结论

- 未发现阻塞源码交付的高风险问题。
- API 路径、响应字段、数据库表结构和迁移版本保持不变。
- 错误文案、权限判断、上传幂等、文件清理与成绩计算均由现有测试覆盖，未发现行为偏移。
- 工作区在整理前已有大批已暂存功能改动；本次整理保留这些改动，未回退或覆盖其业务实现。
- 当前仅有前端主包超过 500 kB 的构建提示，不影响构建成功；拆包不属于本次纯整理范围。
