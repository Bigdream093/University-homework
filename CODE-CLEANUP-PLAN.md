# 原地清理方案（范围收紧版）

日期：2026-09-06。状态：已按用户授权实施本轮原地清理，实际结果见文末。

## 1. 复核结论与硬约束

原方案不能保证“不新增抽象层、不新增功能、不改变结构”。其中提取票据模块、评分 composable、共用持久化函数，以及统一刷新调度，均可能扩大抽象和调用关系；票据到期字段、自动重取与新提示还会扩展接口或行为。本版明确移除这些内容。

本轮只做现有文件内的局部简化、经证据确认的删除、已确认问题的最小修复，以及现有测试与文档的同步。

- 不新建生产代码文件、目录、组件、composable、service、工具模块或公共接口。
- 不移动或重命名生产文件，不调整现有模块依赖方向，不新增跨模块函数导出。
- 不增加页面、按钮、提示、配置项、自动重试、缓存机制或业务流程。
- 不改变 HTTP API、响应字段、数据库结构、草稿格式、上传协议、评分公式与发布版本。
- 不新增依赖，不做版本升级，不进行全仓格式化。
- 不建设通用请求调度、表单管理或上传框架。
- 允许在原文件内用一个不导出的普通函数替代两段完全相同的计算；它不成为新的模块或架构层。若需要策略参数、回调协议或配置对象才能复用，则保留原代码。
- 不为实现“零重复”强行合并业务语义不同的代码。无法在上述约束内安全完成的项目，本轮不实施，不以“确有必要”为例外扩大范围。

## 2. 本轮实施项目

### A. 删除确认无用的旧文件与引用

候选：desktop/package.student.json。

当前该文件版本为 1.6.0，主版本为 1.6.9，仓库搜索未发现消费者。实施前核对隐藏配置、CI、构建脚本、动态路径及打包资源；确认没有用途后删除。发现消费者或无法确认必要外部调用时保留并报告，不迁移外部发布流程。

同时检查无用导入、无消费者导出、废弃组件、旧依赖和样式。仅删除有证据的项目，搜索无命中不单独构成删除依据。动态类名、自动组件解析及脚本入口必须计入检查。

验收：无失效引用，生产构建和相关既有测试通过；删除清单说明每项证据。

### B. 修复评分工作区重复事件

涉及现有 GradeWorkspace.vue、useSummaryWorkspace.js、SubmissionsView.vue，必要时核对 CourseSummary.vue。

现状：“保存并下一位”先发送 saved(当前记录)，有下一位时再发送 saved(下一位, true)。两个父层每次都 load，形成重复请求和竞争。

最小改法：沿用现有 saved(row, advance) 事件签名。有下一位只发送 saved(下一位, true)；没有下一位只发送 saved(当前记录)，并沿用原有关闭逻辑。不增加新事件载荷、导航状态或提示。

合并 SubmissionsView 中两个分支里相同的查找与赋值，保留原有分支条件含义。

验收：一次成功保存只发一次 saved，父层只执行一次 load；有下一位正确切换，末位按原流程关闭；普通保存、退回、失败时输入保留均维持原行为。通过现有测试文件中的回归案例核对两个父层。

本轮不顺带调整小组评分顺序、去重规则、失效记录导航或整个工作区生命周期。其他竞态若仍存在，单独记录，不将这次修复描述为解决所有异步问题。

### C. 简化工作区列表的重复计算

涉及现有 useSummaryWorkspace.js。

两段列表构建都先提取 cell，再 find 原 student。改为直接遍历 student，取得对应 cell，过滤后调用已有 workspaceRowOf；避免 O(n²) 查找。

两处使用同一个文件内不导出的普通函数，只传 assignmentId。保留记录数量、顺序、过滤条件和字段，不添加小组去重、索引缓存或新的数据模型。

验收：同一输入生成与修改前等价的记录列表；个人、小组、未提交和 not_assigned 情况不改变结果；不存在新增导出与文件。

### D. 原地修复预览图 200 张限制

涉及现有 useSubmissions.js；useSummaryPreviews.js 和服务端票据路由只作为已有行为的核对依据。

现状：服务端只处理请求中前 200 个 ID，useSubmissions 却一次发送全部图片。就在现有 redeemPreviewTickets 内将 ID 去重、分批请求并合并结果，再按原方式回填；每批最多 200 个。

不提取共享票据模块，不改变服务端接口，不修改汇总页缓存，不增加到期字段或自动重取。保持请求前捕获的目标列表归属，避免对切换后的列表回填旧结果；沿用现有错误处理。

验收：0、1、200、201、多批与重复 ID；现有权限行为和失败处理保持；每批不超过 200。补充现有测试文件，不建立新测试框架。

这是已有图片显示功能的缺陷修复，可能增加大列表下必要的分批请求次数，但不增加产品功能。

### E. 可选的原地统计简化

涉及现有 useSubmissions.js。

counts 与 stats 复用已有统计结果，不增加公共统计工具、数据层或新导出。必须保留 counts.submitted（待批改）和 stats.submitted（已有提交）的不同口径。

只有代码确实更直观时实施；若增加中间对象和转换步骤而使理解更困难，则保持原状。统计没有性能测量结论，不以此承诺可感知提速。

验收：所有筛选及统计结果保持一致，零分、未提交、已退回、已评分不改变口径。

### F. 文档和现有验证流程同步

在 README 修正 npm test 覆盖范围，修正指向已删除文档的链接，不重建文档体系。

桌面测试继续使用现有 npm test --prefix desktop。本轮验收显式执行该命令。现有 validate-release.yml 可直接增加 desktop 的依赖安装与该测试步骤，不新增 workflow、根脚本别名、workspace 或发布任务。

这会扩大现有 CI 的验证覆盖与运行时间，但不会改变应用结构或产品功能。若独立依赖安装在该 CI 环境不能正确执行，则明确报告，不能以改变构建体系来绕过。

## 3. 从原方案移除的项目

| 原计划 | 本轮决定及原因 |
| --- | --- |
| 新建票据请求模块 | 移除；会增加公共模块及调用层 |
| 票据到期响应字段、缓存有效期、自动重取 | 移除；涉及接口与新运行行为 |
| 提取评分校验、请求和草稿 composable | 移除；会新增跨组件抽象 |
| 合并普通与分片资料写入 | 移除；两条路径事务、幂等、可选文件语义不同，合并会改调用边界 |
| 统一自动/手动刷新调度与排队 | 移除；会引入新的调度机制及状态 |
| 小组工作区去重与导航变化 | 移除；尚需确认业务语义，不能混入等价清理 |
| 改造 saved 事件载荷并增加提示 | 移除；保留原事件约定即可修复已确认重复触发 |
| 拆分大文件或新建 shared 目录 | 移除；不改变项目结构 |
| 新增根 test:desktop 入口 | 移除；直接使用现有桌面测试命令 |

资料写入、评分草稿等已识别重复会继续存在。这是遵守本轮范围的明确取舍，不能在最终报告中宣称“全部重复已消除”。

## 4. 必须保留

- 普通与分片上传，以及无文件提交、旧服务器回退。
- 纯文本渲染与前后端各自的富文本清洗。
- builder 的 cjs 与 yml 配合关系。
- 历史数据库迁移、旧存储路径兼容和上传前后权限复核。
- 已有草稿隔离键、事件参数、模块导出与 API 语义。

## 5. 实施及验收顺序

1. 记录已有工作区变更，复核删除候选。
2. 在原文件内修复评分重复事件、简化工作区列表，执行相关回归。
3. 在原函数内修复票据分批，执行边界测试。
4. 仅在清晰度改善时整理统计，完成确认无用项删除。
5. 同步 README 与现有 CI 步骤，执行最终检查。

修改后的必要验证：根 npm test、桌面 npm test --prefix desktop、前端生产构建、现有浏览器回归；使用现有测试文件增加针对重复事件与超过 200 张图片的行为案例。与本轮改动无关的安装包重建、业务库迁移和大型压测不列为强制验收。

最终 diff 必须满足：

- 无新增生产文件或目录，无文件移动，无新增导出，无新增依赖。
- 无新业务 UI、API 字段、配置、数据库变更或后台机制。
- 无大面积格式变化、调试输出、临时文件或不相关修改。
- 每项修改都能对应本方案的实施项目；移除项目没有夹带实施。
- git diff --check 通过，测试结果与未执行项据实列出。

现有三个 docs 文件删除、AUTO-UPDATE-PLAN.md 未跟踪等用户变更未恢复、覆盖或提交。此前通过的测试仅是修改前基线，不能替代实施后的验证。

## 6. 实际修改记录

- GradeWorkspace：保留 saved(row, advance) 约定，连续评分只发一次事件；原 persist 函数内增加正在保存时的重复调用保护。
- GradeWorkspace：普通保存和退回共用文件内 submit(kind)，删除两段相同的成功通知与关闭处理；校验、请求和失败处理仍由原 persist 负责。
- useGradeSettings：直接以已有 savedConfig 初始化草稿，保留 weights 独立副本，去掉逐字段重复赋值。
- SubmissionsView：合并保存回调中相同的查找与赋值分支。
- useSummaryWorkspace：用文件内非导出函数合并列表生成，直接遍历学生，不再逐行 find 学生；保留列表顺序及小组记录数量。
- useSubmissions：原地按 200 个 ID 分批获取票据并去重，沿用 loadSequence 丢弃过期响应与旧批次；统计复用现有 counts，保持两种 submitted 口径。
- 删除 desktop/package.student.json：仓库脚本、配置与运行代码无消费者，学生端构建使用现有 builder extraMetadata，桌面构建配置测试通过。
- 现有发布验证流程直接运行桌面依赖安装与桌面测试；README 同步实际命令并移除失效文档链接。
- 回归用例加入现有 business-interactions.test.js。评分双事件及票据分批用例先在旧实现上失败，再在修改后通过；另覆盖末位关闭、重复点击、旧票据响应及工作区列表。

首轮生产代码只修改五个既有文件。无新增生产文件、公共导出、依赖、API、数据库变更或新产品功能。跨协议资料持久化合并、草稿抽取、刷新调度改造和票据过期机制均未实施。

验证记录：服务端 88 项、最终前端 84 项、桌面 12 项测试通过；前端 lint 与格式检查通过。首轮生产构建及全部 9 项浏览器测试通过；最后两处等价简化后的生产构建、课程设置与个人/小组流程共 3 项浏览器复验也全部通过。CI 配置已更新，本地未执行远端 Linux CI 或重新制作安装包。

## 7. 后续原地简化：保持原有功能接口

按用户进一步要求，追加两处局部等价整理：

- server/src/routes/previewImages.js：缩略图发送和缺失回退统一在已有 deliverPreview 内处理，登录入口与票据入口直接调用。HTTP 路径、鉴权、票据格式、响应类型、下载文件名与错误映射不变；未新增导出或文件。
- server/src/services/materialService.js：saveMaterialSession 的创建和替换分支共用末尾的结果读取、onSaved 回调和返回。原参数及返回格式不变，写入、旧文件清理入队与完成回调仍处于同一个事务。没有合并普通上传和分片上传协议。

在现有测试文件补充等价验证，先在旧实现通过，再在新实现通过：两种图片访问方式的字节、响应头一致；无缩略图或缩略图文件缺失时回退原图；原图缺失仍可读取已存在缩略图；资料创建和替换的回调失败均回滚，成功回调仅一次。

本轮服务端全部 90 项测试通过，git diff --check 通过。前端与桌面本轮未改动，沿用前一轮验证结果；本轮没有重新运行浏览器或安装包构建。
