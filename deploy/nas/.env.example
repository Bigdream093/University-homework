# Existing NAS: keep your original JWT_SECRET. Never use this example value.
JWT_SECRET=replace-with-your-existing-secret
# Paths are relative to the NAS deployment folder, or absolute NAS paths.
# Both folders must already exist and contain the original data.
DATA_DIR=./data
UPLOAD_DIR=./uploads
APP_PORT=34567
