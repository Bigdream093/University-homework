# 全仓代码与测试统一瘦身方案

日期：2026-09-06。状态：已按方案完成首轮全仓实施与验收。

## 1. 目标及边界

目标是在现有架构内大范围清除重复、失效和机械性的代码。完成后，理解一个业务动作需要看的地方更少，特殊分支更少，原有行为和功能接口保持不变。

本轮是统一审计和分批实施，不是一次性重写。此前局部修复作为当前基线，不计作本轮新增瘦身成果。

硬约束：

- 不新增生产模块、目录、组件、composable、service、抽象层、依赖或产品功能。
- 不改变 HTTP 路径、方法、参数、响应、状态码；不改变组件 props/events、公开导出、Electron IPC/preload 契约、构建命令及配置入口。
- 不改变数据库结构、存储键、草稿键、上传协议、评分口径、权限、事务、幂等、重试和兼容语义。
- 可删除确认无用的内部文件，可将重复代码收敛到已有且职责匹配的实现；不增加跨模块公共接口或代理层。
- 可合并原文件内的普通函数，前提是同时删除重复实现，不引入策略对象、回调协议或通用配置框架。
- 不压缩代码、不缩写业务名称、不合并长行来制造行数收益，不做全仓格式化。
- 不另建测试框架或公共 fixture 模块。优先使用已有测试辅助设施、同文件参数化，以及将同领域用例迁入已有专题测试文件。
- 新发现的 bug 单独记录。不能在等价瘦身中悄悄改变现有行为；需要修复时应明确区分修复提交与等价整理提交。

“全仓”表示每个目录都被检查，不表示每个文件都必须修改。已有接口即使看起来冗余，也不能因仓库内无调用就认定没有外部使用者。

## 2. 当前规模与证据

按当前工作区统计，包括空行和注释；排除依赖、产物、锁文件及非代码文档：

| 核心范围 | 文件数 | 行数 |
| --- | ---: | ---: |
| server/src | 61 | 7,189 |
| web/src | 59 | 8,251 |
| desktop/src | 7 | 1,110 |
| 生产核心合计 | 127 | 16,550 |
| server/test | 17 | 4,328 |
| web/test（含已有辅助代码） | 10 | 1,606 |
| desktop/test | 2 | 232 |
| test/browser（含启动脚本） | 4 | 589 |
| 测试合计 | 33 | 6,755 |

构建、部署、运维脚本与配置也纳入审计，但不混入上述核心代码计数。全仓基线另记录这些文件的数量与行数。

已确认的重复线索：

1. frontend-regressions.test.js 与 helpers/view-harness.js 各自实现 Vue renderer、编译和挂载辅助；前者还承担登录、Pinia 与宿主环境设置，不能不加核对直接替换。
2. file-size、files-list、notices-materials、package 等服务端测试重复设置临时目录、环境变量、数据库与登录环境。
3. unified-workflows.test.js 约 1,571 行，涉及通知、资料、提交、文件、归档、权限、导出等；与专题文件存在主题重叠，但尚未认定具体断言可删除。
4. StudentMaterials.vue 与 CourseMaterials.vue 的下载动作有相同代码；两者已经调用 useDownload。这里继续抽取一层不一定值得，不能为几行去重扩大调用链。
5. 普通与分片资料上传存在相似写入 SQL，两个评分界面存在相似校验和草稿逻辑。事务、可选文件、关闭与草稿语义不同，不列为无条件合并项。

这些只是可核对的候选，不是已经证明可删除的行数。8 行文本块扫描只能发现外观重复，不能代替行为判断。

## 3. 生产代码：按完整业务动作审查

| 业务范围 | 重点文件/目录 | 允许的瘦身方式 | 必须保留的差异 |
| --- | --- | --- | --- |
| 登录、会话与改密 | auth、request、session、user、LoginView、PasswordView | 删除重复读取与无效分支，沿用已有会话实现 | 角色限制、首次改密、回跳地址、并发 401 |
| 课程、学生、作业与分组 | 对应 routes/services、CourseManage 及已有组件 | 删除重复映射和条件，合并同函数内相同后处理 | 归档状态、教师归属、小组成员快照 |
| 提交、普通/分片上传 | submissions、submissionLogic、operations、uploadSessionService、StudentSubmit、两个上传 composable | 原地消除重复状态赋值与成功/失败收尾；现有规则已有入口才复用 | 幂等、版本冲突、暂停取消、故障恢复、上传前后复核 |
| 评分、汇总与导出 | grading、summaryService、gradeScores、评分组件与已有 composable | 消除同口径重复计算、重复分支；直接使用已有结果 | 0 分与未评分、个人/小组、缺交口径、舍入、草稿隔离 |
| 资料、预览与下载 | materials、materialService、previewImages、downloads、useDownload | 同文件内合并相同发送、映射、完成处理 | 浏览器/桌面下载、Range、下载计数、文件清理时机 |
| 通知、问答与富文本 | notices、questions、richText、对应页面 | 清除失效逻辑与重复数据加工，复用已有明确实现 | 发布/撤回、内容修订、已读版本、纯文本/HTML、安全清洗 |
| Electron 与更新 | desktop/src、scripts、builder 配置 | 原地减少重复平台判断和收尾，删除已证实废弃配置 | Windows/Mac、签发角色、版本判断、用户确认、活动下载 |
| 数据、迁移和运维 | db、migrations、storage、server/scripts、deploy | 只清除可证明等价或无效内容 | 历史升级路径、存活文件引用、故障恢复与部署入口 |
| CSS、资源与构建 | styles、组件样式、vite、package、CI | 删除无消费者资源和规则，合并确定等价声明 | 动态类名、scoped 样式、优先级、按需组件、自动扫描与打包路径 |

每个候选先回答：删除了哪一份实现？业务读者少看了哪里？少了哪些分支？用什么证明行为相同？如果只能回答“代码行数少了”，不实施。

## 4. 测试瘦身：先去重复设施，再去重复场景

### 4.1 收敛前端已有测试设施

优先处理 frontend-regressions.test.js 与 helpers/view-harness.js 的重复 renderer、编译、节点遍历、文本读取及卸载清理。

使用已有 view-harness 的能力及现有 dependencies/handler 注入迁移用例。Pinia、userAgent、location、路由 query 等设置保持在相关测试中；必须验证全局状态恢复、异常捕获和真实 store 行为。

若现有 harness 不足以承载个别测试，保留其必要特殊环境，不给 harness 加一串特殊开关，也不新建一层适配器。只有所有消费者都迁移完成后，删除旧辅助实现。

### 4.2 服务端按领域归并重复搭建

不建立全局测试数据库，不从一个 .test.js 导入另一个 .test.js，也不将环境变量初始化放到 app/db 导入之后。

先比对这些候选组：

| 候选组 | 保留位置原则 |
| --- | --- |
| unified-workflows 与 notices-materials | 通知/资料的独立规则归入已有专题文件，跨领域交互保留在流程文件 |
| unified-workflows、submission-policy、business-boundaries | 普通提交规则、权限边界与跨流程故障分别保留，不按关键词粗暴合并 |
| files-list 与 package | 列表字段与单文件访问、ZIP 内容分别保留；重复上传准备在同文件内复用 |
| api 与 grade-scores、unified-workflows | API 绑定和响应契约、纯计算边界、跨流程状态分别保留 |
| upload-streaming、upload-sessions 与浏览器续传 | 流式字节与资源约束、协议状态、真实浏览器连接分别保留 |

将独有用例迁入已经存在且最匹配的专题文件后，才能删除原重复用例或空文件。专题文件内部用清晰的 fixture 复用账号、课程和独立场景创建，保留每个测试的数据隔离；不能借上一条测试产生的业务状态减少准备代码。

不把全部用例塞进 unified-workflows。该文件应逐步留下必要的跨领域交互，已经有专题归属的独立规则迁回专题文件；没有合适现有目的地则留在原处。

### 4.3 每条拟删除测试必须有覆盖对应记录

记录字段：原文件及用例 → 被验证行为 → 输入/角色/状态 → 失败方式 → 保留用例 → 独有断言是否已迁移 → 删除理由。

允许删除：

- 相同入口、相同前提、相同失败方式、相同断言的重复测试。
- 已被真实行为测试覆盖，且只检查旧文件名、源码片段或旧内部调用顺序的脆弱断言。
- 同一规则大量复制的案例正文；改为同文件具名参数化，每个案例仍独立执行和报告失败。
- 已删除内部实现专属、且不再代表有效业务契约的测试。

禁止删除：

- 为 bug 添加的回归用例，除非另一保留用例能捕捉同一缺陷。
- 权限、隐私、并发、事务回滚、幂等、断网、跨平台、数据库升级与物理文件残留等独有边界。
- 仅因服务端测试成功就删除前端事件测试；仅因单元测试成功就删除真实网络、SQLite、文件系统和浏览器接线验证。
- 对平台、流式 I/O 等确有必要、没有其他行为验证手段的约束检查。不能把所有源码检查都视为无价值。

合并场景时不把十个独立测试变成一个长链：前半段失败不应遮蔽后半段所有边界。

### 4.4 各层职责

| 层级 | 主责 | 可减少的重复 |
| --- | --- | --- |
| 纯函数 | 分数、时间、格式、哈希等输入边界 | 用多个完整 HTTP 流程穷举同一公式 |
| 服务端集成 | 权限、状态码、事务、数据与真实文件 | 不断重复普通成功流程作为独立目标 |
| 前端交互 | 事件次数、输入、草稿、响应乱序、按钮与界面状态 | 反复测试后端才负责的业务校验算法 |
| 桌面逻辑/网络 | IPC、平台判断、Range、下载落盘与更新行为 | 相同 mock 成功分支的机械复制 |
| 浏览器 | 关键用户流程和真实系统连接 | 每个界面都重新穷举同一底层边界 |

每种连接边界保留代表性集成用例。数据驱动减少的是正文，不是测试场景或失败可见性。

## 5. 规模目标与停止条件

不预先承诺删除 20% 或 30%；目前没有语义重复统计支持这种数字。大幅度改进来自全仓一致清理，主要收益预计在测试辅助设施和重复准备过程，生产代码可能减幅较小。

第一批完成后，报告实际净减少行数、删掉的重复实现数、辅助代码占比与耗时变化，再给剩余批次的可信区间。不得用删掉功能、边界测试或注释来凑目标。

停止条件：余下重复承担不同契约、无法在现有模块内安全复用，或继续合并会增加分支、跳转、参数和状态耦合。符合停止条件的保留项要解释原因，不继续强行瘦身。

## 6. 执行顺序

1. **冻结基线**：以当前工作区为准记录源码、测试清单、接口和测试结果；识别既有用户变更，保留独立差异。
2. **全仓候选清单**：覆盖核心、脚本、构建、部署、资源；区分已确认重复、独有行为、公开兼容、待核实项。
3. **先瘦测试设施**：收敛已有前端 harness，整理适合归入同一专题的准备代码。此阶段保留所有业务断言。
4. **整理测试归属**：建立覆盖对应记录，迁移独有断言，然后删除确认重复的用例和空文件。
5. **生产代码按业务动作整理**：每批限定一个业务范围；沿现有接口收拢重复处理。不能边删测试边改同一块业务，以免失去对照。
6. **清理资源与脚本**：核实无调用文件、依赖、样式和配置；公开入口与平台配置从严保留。
7. **全量验收和差异复核**：明确完成、保留及未验证项，更新既有说明文档。

阶段 4 后的测试仍须能捕捉原来约束的故障。对高风险合并，可在临时副本重放已知错误，确认保留用例确实失败；不安装新的 mutation 框架。

变更以独立、可回退的批次组织。是否正式提交 Git 或推送按用户授权执行，本方案本身不触发提交、发布或部署。

## 7. 验收门槛

- 本轮增量没有新增生产/测试模块、依赖、公开接口或抽象框架；既有辅助设施可复用但不能膨胀成第二套系统。
- HTTP、组件事件、公开导出、IPC、存储与构建契约保持，格式清理与功能改动不混入。
- 每条被删除测试均有覆盖对应记录；场景总数变化有解释，原独有边界无遗漏。
- 保留测试在独立数据下运行，不依赖执行顺序；全局对象、计时器、环境变量、临时目录和数据库均正确释放。
- 服务端、前端、桌面测试，前端静态检查与生产构建通过；最终运行完整浏览器回归。
- 碰到部署或平台构建配置改动时，执行对应环境验证；未验证的平台不能宣称已通过。
- 统计原始行数及非空行数、文件数、重复实现数、用例数和准备代码体量，使用同一口径前后比较。运行时间仅在同环境同命令下比较。
- 不以覆盖率单个百分比替代行为对应；不新增覆盖率依赖来装饰报告。
- git diff --check 通过；无临时文件、无关格式变化、测试快照批量接受或用户既有改动被覆盖。

## 8. 最终交付

一份改动差异，配合本文件维护四张清单：删除项及证据、测试覆盖对应、接口保持核对、验证结果与保留项。不要再散落创建一组新报告或测试说明体系。

## 9. 首轮实施结果

本轮没有新增生产模块、功能、抽象层、依赖或公开接口。生产核心由 16,550 行降至 16,521 行，净减少 29 行；测试由 6,755 行降至 6,467 行，净减少 288 行并删除 1 个重复测试文件。统计以实施前工作区快照为基线，包括空行和注释。

已完成的收敛：

- 前端回归测试删除自建的 Vue renderer、编译、节点遍历和卸载实现，直接复用已有 `view-harness`；登录专属的 Pinia、路由和宿主环境仍留在原测试内。
- `files-list.test.js` 的独有文件列表断言迁入同领域的 `package.test.js`，共用该文件既有的提交准备；原文件删除，列表、访问控制和 ZIP 断言均保留。
- 通知与资料测试在原文件内统一鉴权请求准备；没有把不同权限或发布状态合并成长链测试。
- 上传中间件的两份同构文件清理、分片会话的三份会话文件清理、Web 普通上传的三份成功收尾均在原模块内收敛。
- 问答保存共用同一请求体；预览图发送、资料保存、评分保存/退回、汇总列表构造和桌面下载失败收尾删除重复分支。
- 删除无引用的 `desktop/package.student.json`，并让现有发布验证工作流执行桌面测试；README 的测试说明同步到实际入口。

测试覆盖对应：

| 删除或收敛项 | 保留验证 |
| --- | --- |
| `files-list.test.js` | 两条独有用例原样迁入 `package.test.js`，并与打包下载测试共用独立 fixture |
| 前端自建挂载设施 | 原 28 条 `frontend-regressions` 用例继续执行；交互回归仍由 `business-interactions` 覆盖 |
| 上传成功与清理重复分支 | 普通上传、分片会话、流式上传、真实浏览器断点续传和桌面真实下载继续分层验证 |
| 评分与预览重复后处理 | 新增事件次数、并发保存、乱序票据及 0/1/200/201/401 张边界回归 |
| 资料保存重复收尾 | 普通与分片创建/更新、失败回滚及文件清理继续由服务端集成测试验证 |

接口核对使用 AST 对实施前快照与当前生产代码逐文件比较，共检查 123 个 JS、CJS 和 Vue 文件的公开导出、组件 props/events/expose、Express 路由方法与路径、Electron IPC 和 preload 暴露签名，未发现契约变化。数据库结构、存储键和上传协议未修改。

最终验证结果：服务端 90 项、前端 84 项、桌面端 12 项和 Playwright 浏览器 9 项全部通过；前端 lint、Prettier 检查、Web 生产构建、Windows 教师端与学生端安装包构建、`git diff --check` 均通过。

停止审计保留了普通/分片上传、学生/教师资料、评分入口，以及纯函数、HTTP、前端交互、桌面网络、真实浏览器之间的相似代码。这些位置分别承担事务、角色、平台或连接边界；继续合并会新增参数分支、跨模块跳转或通用代理，违反本轮判断标准。
