# 测试、源码与 1.6.0 发布包核验

> 以下审计正文记录修复前状态。用户随后批准修改业务源码，成绩舍入修复及验证结果见文末“后续源码修复”；原有发布包未重建。

审计基线：`f25e45c3229b290816c88d475de042f1293b760f`，分支 `fix/review-hardening`。
本轮新增真实浏览器测试、测试依赖/配置及文档；没有修改 `server/src`、`web/src`、`desktop/src`、Dockerfile 或已有发布包。

## 测试结果和可确认的业务缺陷

原有 `npm test` 实测 143 条通过：后端 67、前端组件/工具 53、桌面逻辑 8、跨层集成 15，失败/跳过均为 0。`npm run build` 通过。
新增 5 条 Chromium 浏览器测试，案例总数为 148；同一案例在源码与发布包各执行一次不算两个案例。

浏览器最终结果见本目录说明和 `test-results/browser-source-report/`、`test-results/browser-release-report/`。

源码生产构建上的最终浏览器结果：**4 条通过、1 条失败**（1.3 分钟）。失败项仅为成绩舍入边界；个人/小组完整链路、实际暂停续传、在线断网重试均通过。加上原有测试，源码侧共 147 条通过、1 条失败，没有跳过。

直接从发布 tar 加载的 NAS 容器最终结果同样为 **4 条通过、1 条失败**（1.2 分钟），唯一失败项及实际值与源码侧一致。两个浏览器运行均以非零状态退出，保留失败截图、trace 和真实下载的 XLSX。测试容器在核验后删除，原始发布 tar/EXE/DMG 未重写。

| 问题 | 证据及影响 | 建议 | 本轮处理 |
| --- | --- | --- | --- |
| P1：成绩页面与导出舍入不一致 | 平时占比 100%、单项满分 100、评分 2.55，实际页面显示 2.5，实际下载 XLSX 总成绩为 2.6。`web/src/components/CourseSummary.vue` 的 `formatScore` 用 `toFixed(1)`；`server/src/services/summaryService.js` 用 `Math.round(value * 10) / 10`。二进制浮点边界触发差异 | 确定统一舍入规则，让页面与导出采用同一实现/服务端结果；修复后复跑本边界用例和汇总主链路 | 新增真实浏览器失败回归，未跳过、未标记预期失败；依照用户要求不修改业务源码 |
| P2：原组件测试不能证明浏览器真实交互 | 原 `view-harness.js` 使用自定义 Vue renderer，替换 Element Plus、文件选择组件与部分下载；原成绩一致性测试仅 6 个向量，未覆盖 2.55 | 保留组件测试用于快反馈，补充真实 DOM、文件输入、登录路由、下载文件验证，并保留独立数学预期 | 新增个人/小组完整链路、在线断网重试、暂停续传和舍入边界 5 条用例 |
| P2：Docker 构建不遵循根锁文件 | Dockerfile 分别复制 `web/package*.json`、`server/package*.json` 后执行 `npm install`，但仓库锁文件在根目录。实际本机/新建 Linux 构建依赖不同：Vue 3.5.41/3.5.42、Axios 1.19.0/1.20.0、SortableJS 1.15.6/1.15.7 | 按 npm workspaces 布局使用根锁文件及 `npm ci`，锁定基础镜像并记录依赖/源码提交/包 SHA-256 | 仅核实记录；未改变构建或重新发布 |

新增测试自身已校正两个定位陷阱：登录 URL 必须检查真实 pathname，避免 `/password?redirect=/teacher/...` 被误认成功；成绩断言取当前学生行的实际总成绩列，避免仅依赖不唯一的 CSS 类名。没有通过降低成绩期望值来消除真实业务失败。

## 发布包核验

| 产物 | 静态比对 | 实际运行 | 结论与边界 |
| --- | --- | --- | --- |
| NAS `university-homework-1.6.0-linux-amd64.tar` | 标签 `university-homework:1.6.0`，Linux amd64；包内 50 个业务服务端源码文件、21 个帮助文件与当前工作区一致。未把本机 `.mimosa` 工具文件计入源码。前端 30 个产物与本机/新建 Linux 构建的文件摘要并不完全一致；新建 Linux 构建确认依赖漂移 | 实际 `docker load`，启动独立生产模式容器，映射随机本机端口；不挂载用户 data/uploads。同一套浏览器用例 4 通过、1 失败 | 主要链路通过，成绩舍入缺陷在包内同样存在；当前证据不足以宣称前端发布产物可从锁定源码逐字节重现。不能代表真实 NAS 硬件/网络验收 |
| Windows 教师 EXE | 直接解开发布安装器的 `app-64.7z`，读取其中 `app.asar`；5 个 `desktop/src` 文件与工作区逐字节一致，角色 teacher、版本 1.6.0 正确 | 用安装器内解出的真实程序及独立用户配置启动，实际点击“测试连接”“保存并进入”，连接 NAS 测试容器进入登录页，UA 含 `KexuTeacher/1.6.0` | 应用载荷启动和服务器连接通过；未执行系统安装/卸载向导 |
| Windows 学生 EXE | 同上，5 个源码文件一致，角色 student、版本 1.6.0 正确 | 独立配置启动、连接测试、保存、进入 NAS 登录页，UA 含 `KexuStudent/1.6.0` | 应用载荷启动和连接通过；未执行系统安装/卸载向导 |
| macOS arm64 学生 DMG | 解包读取 `app.asar`；5 个源码文件与 Git HEAD 中内容逐字节一致，与 Windows 工作区归一化换行后相同；角色 student、版本 1.6.0。另从 GitHub Actions 重新下载 DMG，与本地发布文件 SHA-256 完全一致 | 当前机器为 Windows，未执行 macOS 原生运行 | 源码来源及下载产物一致性通过；不能宣称 macOS 安装、签名/Gatekeeper、运行及下载行为通过 |

桌面包只包含客户端连接、窗口与下载逻辑，业务网页由配置的服务器提供。EXE/DMG 的源码一致不等于已包含相同版本的前后端业务代码，需同时核验所连接的 NAS 服务。

包 SHA-256：

| 产物 | SHA-256 |
| --- | --- |
| NAS tar | `10974c06ca7e1b555ae9c53761cb85e2da0714aadb61a27e4015473c3f3da637` |
| 教师 EXE | `394d1007cf698ac6b5ef32b41e54276cf8a38887bcd11162cc4dea1ec1e57105` |
| 学生 EXE | `28b8410bd5de2efbe2d965f533a2dcd0fed183972edcf8795697f8d2281aa19e` |
| 学生 DMG | `ffdb8070bb33e8385a3b96c912d0b04bbf75bfaa978bf258106beb7ae7dcd720` |

## GitHub 与 README

- 审计开始时工作区干净，本地 HEAD 与远端 `fix/review-hardening` 均为 `f25e45c3229b290816c88d475de042f1293b760f`。
- 默认 `main` 为 `ef1f672fbee1c31aa5dfec549af2d87c2c7c622b`。两个分支已分叉：当前分支有 3 个 main 尚未包含的提交，main 有 1 个当前分支尚未包含的提交；不能说主分支已同步。
- [macOS 构建记录](https://github.com/Bigdream093/University-homework/actions/runs/33834777972) 成功，`head_sha` 为上述 f25e45c；artifact ID 为 `9922927807`。本地 DMG 与这次下载的产物完全一致。
- GitHub Releases 查询为 0，未查到 `v1.6.0` 标签。`release/` 被 Git 忽略；本地发布包、Actions artifact 和 GitHub 正式 Release 是不同状态。
- 审计开始时 README 的 Git blob 与远端当前分支相同（`261063d4e2f93c5e0457725480fa520494697864`），但缺少真实浏览器测试步骤和覆盖边界。本轮已在本地补充测试入口、边界缺陷及报告链接，并校正默认上传限额说明、补充桌面依赖安装步骤。
- 本轮新增测试与 README 更新仍在工作区，尚未 commit/push；没有合并 main、打标签或发布新包。

建议下一步先处理成绩舍入缺陷和构建锁定问题，再完成 main 合并/测试、使用同一提交重建各包并附校验清单。当前不作“源码、三端包、全部测试和 GitHub 发布均已完成”的结论。

## 后续源码修复（2026-09-04）

用户批准后，已修复 `web/src/components/CourseSummary.vue` 的 `formatScore`：先执行与服务端相同的 `Math.round(value * 10) / 10`，再用 `toFixed(1)` 补足小数位。平时、期末和总成绩显示均采用此函数，原始评分和中间加权计算不变。

新增跨层向量验证平时、期末和总分在 2.55 边界均显示 2.6。成绩一致性、业务页面组件和学生真实后端链路共 **33 条定向测试通过**；`npm run test:browser` 重新构建后 **5 条浏览器测试全部通过**（55 秒），包含原先失败的真实页面/Excel 舍入回归，无跳过。

源码浏览器报告已更新为修复后的结果；旧发布包浏览器报告仍保留原来的失败证据。README 已同步修复状态。构建锁定问题、发布包重建、提交/推送及 main 合并尚未处理，不能把源码修复结果当作现有发布包已修复。
